Multi-bank coverage + "Mandates we caught (full list)" drawer

Two linked improvements to how the tool credits and shows caught mandates.

1) MULTI-BANK SCHEMA (fixes co-adviser undercounting)
- Stories/opps can now carry banks: [...] (lead first) alongside the legacy
  single bank string. A co-advised deal surfaces for EVERY rep who covers any
  bank on it, and the mandate count credits each advising bank.
- Shared itemBanks()/isMyBankAny() (utils.js) + advisingBanks() (brief-helpers)
  normalize both shapes; all readers iterate the list. Backward compatible —
  old single-bank editions are unchanged.
- Coverage filters, YOUR BANK badges, coverage-gap note, opp card label, and
  outreach modal title all handle multi-bank. Also fixed a latent
  isMyBank(undefined) throw on the opp path.
- Tally (build-bank-trackers.js + tally-mandates.js, kept in lockstep) credits
  every bank on a deal. carryForward keys on the PRIMARY bank so co-adviser
  changes don't fragment a running deal's identity.
- generate-brief.js schema now emits banks: [...] with a rule to list all
  covered advisers on both sides.

2) "MANDATES WE CAUGHT (FULL LIST)" DRAWER
- build-bank-trackers.js now emits caught_mandates[] per bank (deduped by the
  same key as the counts, newest-first) — list length === mandates_total.
- Collapsible drawer on each bank card (pages.js + main.css): 15 newest with a
  "show all N" expander, each row showing date, confidence badge, and source
  link. Makes the tracker numbers auditable.

DATA FIXES:
- Normalized "Centerview"->"Centerview Partners" and "Guggenheim"->"Guggenheim
  Securities" across editions (name mismatch was undercounting both).
- Retagged edition 20's SEGRO/Williams/Atkore items with banks[] so co-advisers
  (UBS/Goldman/Morgan Stanley on SEGRO, Barclays/Jefferies on Williams) now
  surface. UBS gains a tracker; GS/MS/Centerview/Guggenheim counts corrected.

Cache version bumped to 20260804-1 across index.html + all import URLs.
50 tests pass (46 + 4 new multi-bank cases), link check clean, no mojibake.
