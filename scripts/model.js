/* ============================================================
   model.js — The Claude model used by all generation scripts.
   Single source of truth so generate-brief.js and generate-market.js
   can't drift apart.
   ============================================================ */

export const MODEL = 'claude-sonnet-4-6';
